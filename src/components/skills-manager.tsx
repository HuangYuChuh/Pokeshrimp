"use client";

/**
 * @deprecated Skills management is now integrated into the Settings dialog
 * under the "Skills" tab. Open SettingsDialog with initialTab="skills" instead.
 *
 * This component is no longer used and exists only as a placeholder.
 */
export function SkillsManager(_props: { open: boolean; onClose: () => void }) {
  return null;
}
