# Code Review Checklist — Issue #97

Temporary review document. Delete after PR merges.

## Karpathy Criteria

- [ ] **No hidden assumptions** — every design choice traces to issue spec or explicit rationale
- [ ] **Minimum code** — no speculative abstractions, no `accept` param on hook (single consumer), no `mouseleave` escape hatch (unverified benefit)
- [ ] **Surgical scope** — only files listed in the plan are touched; no drive-by cleanups
- [ ] **Verifiable success** — each step has a concrete check (test green / manual repro)

## Project Convention Criteria (CLAUDE.md)

- [ ] Hook file: `src/hooks/use-drop-zone.ts` (kebab-case)
- [ ] Test file: `src/hooks/__tests__/use-drop-zone.test.ts` (co-located `__tests__/`)
- [ ] `"use client"` directive on all client-side files
- [ ] Imports use `@/` path alias
- [ ] No framework imports in `src/core/`
- [ ] Icons: Iconify + Solar only
- [ ] Design system tokens only (no hardcoded colors/spacing)
- [ ] i18n: all user-facing strings go through `useT()`
- [ ] TypeScript strict — no `any`, no type assertions without justification
- [ ] Functions < 20 lines where possible
- [ ] < 3 levels of indentation
- [ ] One component per file

## Functional Criteria (from Issue #97)

- [ ] ESC keypress dismisses overlay
- [ ] Click on backdrop dismisses overlay
- [ ] `window blur` (alt-tab) resets drag state
- [ ] `dragend` resets drag state
- [ ] Normal drag-in + drop `.skill.md` still works (happy path)
- [ ] Non-file drags (text selection) do NOT trigger overlay
- [ ] Nested dragenter/dragleave (child elements) — overlay stays until counter hits 0
- [ ] Overlay shows ESC hint when visible

## Verification Steps

```
1. useDropZone hook created
   → verify: 9 unit tests green

2. SkillDropOverlay updated
   → verify: ESC + click tests green

3. page.tsx simplified
   → verify: npm run dev:web manual test
     a) drag file in → overlay appears
     b) press ESC → overlay gone
     c) alt-tab away → overlay gone
     d) drop .skill.md → toast shows

4. CI pipeline
   → verify: tsc && lint && format:check && vitest run
```

## Files Changed

| File | Change |
|------|--------|
| `src/hooks/use-drop-zone.ts` | NEW |
| `src/hooks/__tests__/use-drop-zone.test.ts` | NEW |
| `src/components/skill-drop-overlay.tsx` | MODIFY |
| `src/app/page.tsx` | MODIFY |
| `src/lib/i18n/en.ts` | MODIFY |
| `src/lib/i18n/zh.ts` | MODIFY |
